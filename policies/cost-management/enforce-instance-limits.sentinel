# Always True aaaPolicy
# This is a simple Saentinel policy that always passes/returns true
# Useful for testing or as a placeholder policy

# Define a simple boolean variable
always_pass = true

# Main rule that always evaluates to true
main = rule {
    always_pass
}

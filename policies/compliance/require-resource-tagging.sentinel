# This is a simplae Sentinel policy that always passes/returns trueaa
# Useful for teasting or as a placeholder policy

# Define a simple boolean variable
always_pass = false

# Main rule that always evaluates to true
main = rule {
    always_pass
}
